<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cuelinksdemo.demo">

    <application
        android:name=".CuelinksApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data android:name="com.cuelinks.publisherId" android:value="11767CL10691"/>
        <meta-data android:name="com.cuelinks.internalUrl" android:value="desidime.com"/>
        <meta-data android:name="com.cuelinks.subId" android:value="appLevel"/>


        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>